  <%= form_with url: search_path, method: :get, local: true, class: "search_container" do |f| %>
  
    <%= f.select :model, options_for_select({ "ユーザーから" => "user", "募集から" => "body" }) %>    
    <!--%= f.select :model, options_for_select({ "ユーザー" => "user", "募集" => "body", "遊ぶ日" => "time" }) %>    -->
    <%= f.text_field :content, placeholder: " 検索" %>
    <!--%= f.submit :class=>"submit" do %>-->
    <%= button_tag class: "search_submit" do  %>
      <i class="fa-solid fa-magnifying-glass"></i>
    <% end %>
  <% end %>